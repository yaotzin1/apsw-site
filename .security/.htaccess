# Never serve anything from this directory over HTTP.
# Apache 2.4+
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>

# Apache 2.2 / mod_access_compat
<IfModule !mod_authz_core.c>
    Deny from all
</IfModule>
